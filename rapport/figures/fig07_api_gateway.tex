% Figure : architecture API Gateway + SSE + UI
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=10mm and 16mm, font=\small]

  % Three layers
  \node[bigagent, fill=cHumanBg, draw=cHuman, text width=3.6cm] (ui) {NiceGUI\\\footnotesize\itshape :8080};
  \node[bigagent, right=of ui, text width=3.6cm] (api) {FastAPI Gateway\\\footnotesize\itshape :8000};
  \node[bigagent, right=of api, fill=cStoreBg, draw=cStore, text width=3.6cm] (graph) {LangGraph\\\footnotesize\itshape worker thread};

  % Endpoints (small boxes inside API)
  \node[smallagent, below=of api, xshift=-12mm] (post) {\texttt{POST /recruter}};
  \node[smallagent, below=14mm of api]            (sse)  {\texttt{GET /stream} (SSE)};
  \node[smallagent, below=of api, xshift=12mm]   (hitl) {\texttt{POST /hitl}};

  % Flèches
  \draw[flow] (ui) -- node[above, font=\footnotesize]{HTTP} (api);
  \draw[flow] (api) -- node[above, font=\footnotesize]{thread} (graph);
  \draw[flowback] (api) -- node[below, font=\footnotesize]{SSE events} ($(ui.east)+(0,-3mm)$);

  % Endpoints lien API
  \draw[faint] (api) -- (post);
  \draw[faint] (api) -- (sse);
  \draw[faint] (api) -- (hitl);

  % decoration : queue
  \node[noteblock, below=of sse, text width=10cm]
       {Pont synchrone~\(\rightarrow\) asynchrone~: \texttt{queue.Queue} \emph{thread-safe}.\\
        Le \emph{worker} pousse des événements typés (\texttt{run\_started}, \texttt{node\_completed}, \texttt{awaiting\_hitl}, \texttt{run\_done}) ; le générateur SSE les consomme par sondage non bloquant.};
\end{tikzpicture}
\caption[Architecture API Gateway + SSE + NiceGUI]{Architecture API Gateway. L'UI NiceGUI ne dialogue qu'avec l'API REST, jamais avec le graphe LangGraph. Le streaming temps réel utilise des Server-Sent Events ; le HITL passe par un endpoint POST distinct.}
\label{fig:api_gateway}
\end{figure}
