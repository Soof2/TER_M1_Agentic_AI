% Diagramme d'architecture de l'application (vue composants)
\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=8mm and 14mm, font=\small]

  % Couche presentation
  \node[uccomponent, fill=cHumanBg, draw=cHuman, text width=3cm] (ui)
       {Interface NiceGUI\\\footnotesize\itshape (5 pages, port 8080)};

  % Couche service
  \node[uccomponent, right=24mm of ui, text width=3.4cm] (api)
       {API Gateway FastAPI\\\footnotesize\itshape REST + SSE (port 8000)};

  % Couche orchestration
  \node[uccomponent, right=22mm of api, text width=3.6cm, fill=cAgentBg!70] (graph)
       {Pipeline LangGraph\\\footnotesize\itshape A1 -- A8 (8 agents)};

  % Outils en dessous
  \node[uccomponent, below=20mm of graph, xshift=-26mm, text width=3.0cm] (search)
       {Outils \emph{search}\\\footnotesize\itshape DDG, GitHub, SO};
  \node[uccomponent, below=20mm of graph, text width=3.0cm, fill=cStoreBg, draw=cStore] (rag)
       {Mémoire RAG\\\footnotesize\itshape ChromaDB local};
  \node[uccomponent, below=20mm of graph, xshift=26mm, text width=3.0cm, fill=cStoreBg, draw=cStore] (llm)
       {LLM Ollama\\\footnotesize\itshape local, port 11434};

  % Stockage logs
  \node[storage, below=12mm of api, minimum width=2.4cm] (logs) {logs / metriques\\\texttt{./logs/*.json}};

  % Sources externes
  \node[uccomponent, below=22mm of search, text width=3.0cm, draw=cData, fill=cDataBg] (ext)
       {Sources publiques\\\footnotesize\itshape DDG, GitHub, Stack Ex.};

  % Recruteur humain
  \node (humain) at ([yshift=4mm]ui.north) {\ucactorperson{Recruteur}};

  % Fleches principales
  \draw[flow] (humain.south) -- (ui.north);
  \draw[flowboth] (ui) -- node[above, font=\footnotesize]{HTTP / SSE} (api);
  \draw[flowboth] (api) -- node[above, font=\footnotesize]{thread} (graph);

  \draw[flow] (graph) -- (search);
  \draw[flow] (graph) -- (rag);
  \draw[flow] (graph) -- (llm);

  \draw[flow] (search) -- (ext);

  \draw[faint] (api) -- (logs);
  \draw[faint] (graph) -| ($(logs.east)+(2mm,0)$);

  % Couches englobantes
  \begin{scope}[on background layer]
    \node[group, fit=(ui), label={[font=\footnotesize\itshape,gray, anchor=south]north:Présentation}] {};
    \node[group, fit=(api)(logs), label={[font=\footnotesize\itshape,gray, anchor=south]north:Service / Gateway}] {};
    \node[group, fit=(graph)(search)(rag)(llm),
          label={[font=\footnotesize\itshape,gray, anchor=south]north:Orchestration et capacités}] {};
  \end{scope}
\end{tikzpicture}
\caption[Architecture en composants de l'application]{Architecture en composants de l'application. Trois couches successives séparent la présentation (NiceGUI), le service (API Gateway FastAPI) et l'orchestration (LangGraph). Les capacités externes (recherche web, mémoire vectorielle, LLM) sont mobilisées par le pipeline de manière encapsulée. Les exports d'observabilité passent par le service.}
\label{fig:archi_appli}
\end{figure}
